{
  "navigationFallback": {
    "rewrite": "/index.html",
    "exclude": ["/api/*", "/images/*.{png,jpg,gif,ico}", "/*.{css,scss,js,map,json,ico,png,jpg,jpeg,gif,svg,webp,woff,woff2,wasm}"]
  },
  "mimeTypes": {
    ".json": "application/json"
  }
}
